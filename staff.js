var counter = function(arr){
    return 'length : '+ arr.length
}

var adder = function(a,b){
    return `sum is equal ${a*b}`
}

var pi  =3.123



module.exports = {
    counter: counter,
    adder: adder,
    pi: pi
}